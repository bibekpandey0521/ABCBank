@using Common.Enums;
<MudForm @ref="@_form" Model="TransactionRequest" Validation="@(_validator.ValidateValue)" ValidationDelay="0">
    <MudDialog>
        <TitleContent>
            Account Transaction
        </TitleContent> 
        <DialogContent>
            <MudGrid>
                <MudItem xs="12" sm="12" md="12">
                    <MudNumericField @bind-Value="TransactionRequest.CurrentBalance"
                                     Label="Curent Balance"
                                     For="@(() => TransactionRequest.CurrentBalance)"
                                     Margin="Margin.Dense"
                                     Adornment="Adornment.Start"
                                     AdornmentText="R"
                                     AdormentColor="Color.Info"
                                     ReadOnly="true"
                                     Variant="Variant.Outlined" />
                </MudItem>
                <MudItem xs="12" sm="6" md="6">
                    <MudNumericField @bind-Value="TransactionRequest.Amount"
                                     Label="Transaction Amount"
                                     For="@(() => TransactionRequest.Amount)"
                                     Margin="Margin.Dense"
                                     Adornment="Adornment.Start"
                                     AdornmentText="R"
                                     AdormentColor="Color.Info"
                                     Variant="Variant.Outlined" />
                </MudItem>
                <MudItem xs="12" sm="6" md="6">
                    <MudSelect @bind-Value="TransactionRequest.Type"
                               Label="Transaction Type"
                               For="@(()=> TransactionRequest.Type)"
                               Margin="Margin.Dense"
                               Variant="Variant.Outlined">
                        @foreach (TransactionType type in Enum.GetValues(typeof(TransactionType)))
                        {
                            <MudSelectItem Value="@(type)">@type</MudSelectItem>
                        }
                    </MudSelect>
                </MudItem>
            </MudGrid>
        </DialogContent>
        <DialogActions>
            <MudButton OnClick="Cancel" Class="mr-auto">Cancel</MudButton>
            <MudButton Color="Color.Success" Class="me-4" OnClick="SubmitAsync"
                       Variant="Variant.Filled"
                       Disabled="@(!_validator.Validate(TransactionRequest).IsValid)">Save</MudButton>
        </DialogActions>
    </MudDialog>
</MudForm>